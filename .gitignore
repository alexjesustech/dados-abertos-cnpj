.env
.venv
.idea
.pytest_cache
temp/
temp_smoke/
extracted/
drivers/
__pycache__/
*.pyc
*.db
*.db-*
dados-abertos-cnpj.log

# pytest-cov + setuptools build artifacts
.coverage
.coverage.*
*.egg-info/
build/
dist/
htmlcov/

# scratch runtime
run.out

# monitor/ — artefatos de runtime (lockfiles, logs, snapshot vivo)
monitor/.collect.pid
monitor/.collect.offset
monitor/.http-server.pid
monitor/.http-server.log
monitor/collect.log
monitor/notifications.log
monitor/notify.sent
monitor/status.json

# Tooling de agente de IA — local-only (a orientação pública vive no AGENTS.md)
.claude/
.opencode/
.agents/
CLAUDE.md
GEMINI.md
opencode.json

# Preferências pessoais de projeto na raiz (se você utilizar)
CLAUDE.local.md

# Segredos: NENHUM *.sops.yaml versionado — nem o ciphertext (sem forward
# secrecy no histórico) nem o .sops.yaml de config (expõe os recipients age).
*.sops.yaml

# Estado de sessão entre agentes — local-only
docs/HANDOFF.md
docs/HANDOFF-arquivo.md
