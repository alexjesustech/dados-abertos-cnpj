.env
.venv
.idea
.pytest_cache
temp/
temp_smoke/
extracted/
drivers/
__pycache__/
*.pyc
*.db
*.db-*
dados-abertos-cnpj.log

# pytest-cov + setuptools build artifacts
.coverage
.coverage.*
*.egg-info/
build/
dist/
htmlcov/

# scratch runtime
run.out

# monitor/ — artefatos de runtime (lockfiles, logs, snapshot vivo)
monitor/.collect.pid
monitor/.collect.offset
monitor/.http-server.pid
monitor/.http-server.log
monitor/collect.log
monitor/notifications.log
monitor/notify.sent
monitor/status.json

# Tooling de agente — local-only em repositório público (diretriz da ambiente local 2026-06-12)
.claude/
.opencode/
.agents/

# Preferências pessoais de projeto na raiz (se você utilizar)
CLAUDE.local.md

# Política 2026-06-12 (ambiente local): NENHUM *.sops.yaml versionado — nem o ciphertext
# (sem forward secrecy no histórico) nem o .sops.yaml de config (expõe recipients age).
# Backup = valores no cofre de segredos Personal Vault (note "env — dados-abertos-cnpj").
*.sops.yaml

# Estado de sessão entre agentes — local-only (curadoria de publicação)
docs/HANDOFF.md
docs/HANDOFF-arquivo.md
