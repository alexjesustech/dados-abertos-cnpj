> Fonte canônica: seção "Sessão (dois canais)" de `AGENTS.md` (raiz do projeto).
> Este arquivo é carregado automaticamente pelo Claude Code e contém apenas
> detalhes operacionais específicos do Claude. A regra de negócio/projeto é o AGENTS.md.

- Mensagem de commit em pt-BR, imperativo (adiciona X, corrige Y, remove Z).
- Sem regressões em testes, logs novos em pt-BR.
- Mantenha `README.md` + `CHANGELOG.md` atualizados (Definition of Done).
